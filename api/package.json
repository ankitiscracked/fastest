{
  "name": "api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 execute fastest-db --local --file=./src/db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@fastest/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260117.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.59.2"
  }
}
